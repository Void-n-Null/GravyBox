# ─── Godot 4 ───────────────────────────────────────────────────────────────
# Editor-managed import/cache data. Regenerated on open; never commit.
.godot/
# Mono/.NET glue cache (older Godot layout)
.mono/
# Exported builds
/build/
/dist/
*.translation

# ─── .NET / C# ─────────────────────────────────────────────────────────────
bin/
obj/
*.user
*.suo
*.userprefs
.vs/

# ─── Rust kernel (future /kernel/) ─────────────────────────────────────────
target/
**/*.rs.bk
*.pdb

# ─── Local-only operating contract (never published) ──────────────────────
# The Lead/Agent contract. Intentionally untracked; see its "About this file"
# section. Present on disk at the repo root even though git ignores it.
AGENTS.md

# ─── OS / editor cruft ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.idea/
